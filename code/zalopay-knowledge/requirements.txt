# ZaloPay Knowledge Agent — pinned runtime dependencies (Python 3.11)
# Upper-bound on major versions to prevent breaking changes during automated installs.
#
# IMPORTANT: The following packages are added automatically at deploy time by
# the GreenNode AgentBase deployment pipeline (do NOT install locally):
#
#   greennode-agentbase>=0.1,<1
#   greennode-agent-bridge[langgraph]>=0.1,<1
#
# Do not uncomment those lines here; they are only used on AgentBase.

# --- Web framework ---
fastapi>=0.111,<1
uvicorn[standard]>=0.30,<1

# --- Validation & configuration ---
pydantic>=2.7,<3
pydantic-settings>=2.3,<3
python-dotenv>=1,<2

# --- LangGraph / LangChain core ---
langgraph>=0.2,<0.3
langchain-core>=0.3,<0.4
langgraph-checkpoint-sqlite>=1,<3

# --- LLM client (OpenAI-compatible, targets VNG MaaS) ---
openai>=1.40,<2

# --- Retry / resilience ---
tenacity>=8.2,<9

# --- Vector index ---
faiss-cpu>=1.8,<2

# --- Embeddings (local, multilingual VI+EN) ---
sentence-transformers>=3,<4
numpy>=1.26,<2

# --- HTTP client (Confluence, internal calls) ---
httpx>=0.27,<1

# --- Google Drive ingestion ---
google-api-python-client>=2,<3
google-auth>=2,<3

# --- PDF extraction ---
pypdf>=4,<6

# --- HTML/Confluence storage-format extraction ---
beautifulsoup4>=4,<5
lxml>=5,<6
markdownify>=0.13,<1

# --- Language detection ---
langdetect>=1,<2

# --- Prompt loader ---
PyYAML>=6,<7
